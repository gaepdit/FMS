@page "{id:Guid?}"
@using FMS.Pages.Maintenance
@model FMS.Pages.Maintenance.SoilStatus.EditModel

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

@{
    ViewData["Title"] = $"Edit {MaintenanceOptions.SoilStatus}: {Model.SoilStatus.Name}";
}
<h1>
    @ViewData["Title"]
    @if (!Model.SoilStatus.Active)
    {
        <span class="text-muted">(Removed)</span>
    }
</h1>

<div class="container bg-light-subtle py-3 rounded-3">
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
        <input type="hidden" asp-for="SoilStatus.Active" />
        <input type="hidden" asp-for="Id" />
        <div class="row mb-3">
            <div class="col-md-3">
                <label asp-for="SoilStatus.Name">@MaintenanceOptions.SoilStatus <span class="text-danger">*</span></label>
                <input asp-for="SoilStatus.Name" class="form-control" aria-describedby="FileIdHelpBlock" />
                <span asp-validation-for="SoilStatus.Name" class="text-danger"></span>
            </div>
            <div class="col-md">
                <label asp-for="SoilStatus.Description">@MaintenanceOptions.SoilStatus Description <span class="text-danger">*</span></label>
                <input asp-for="SoilStatus.Description" class="form-control" />
                <span asp-validation-for="SoilStatus.Description" class="text-danger"></span>
            </div>
        </div>
        <hr />
        <p class="text-danger">* denotes a required field</p>
        <button type="submit" class="btn btn-primary col-sm-3 mb-3 mb-sm-0">Save Changes</button>
        <a asp-page="Index" class="btn btn-outline-secondary col-md-2">Cancel</a>
    </form>
</div>