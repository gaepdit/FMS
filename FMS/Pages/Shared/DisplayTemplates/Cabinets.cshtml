@using FMS.Platform.Extensions
@model List<string>
@if ((bool) ViewData["IsRetained"])
{
    @if (Model.Count > 0)
    {
        @Model.ConcatNonEmpty(", ")
    }
    else
    {
        <span class="text-muted">none</span>
    }
}
else
{
    <span class="text-nowrap">Not retained</span>
}