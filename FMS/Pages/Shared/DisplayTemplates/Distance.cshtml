@using System.Globalization
@model double
@if (Model < 0.25)
{
    @Math.Round(Model * 5280).ToString("N0", CultureInfo.InvariantCulture)
    <text> ft</text>
}
else
{
    @Math.Round(Model, 2).ToString("N2", CultureInfo.InvariantCulture)
    <text> mi</text>
}